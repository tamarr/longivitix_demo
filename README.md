# LongivitiX

Health risk prediction demo — React + Express + OpenAI.

## Setup

```bash
# Server
cd server
cp .env.example .env   # add your OpenAI key
npm install
npm run dev

# Client (separate terminal)
cd client
npm install
npm run dev
```

Client runs on `http://localhost:5173`, server on `http://localhost:3001`.
